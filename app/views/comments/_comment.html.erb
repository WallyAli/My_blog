


<p><%= content_tag :strong, comment.commenter %></p>
<p><em>created <%= time_ago_in_words(comment.created_at) %> ago</em></p>

<%= content_tag :p, comment.body %>

<%= link_to 'Delete comment', [comment.post, comment], 
        method: :delete, data: { confirm: 'Are you sure?' } %>
