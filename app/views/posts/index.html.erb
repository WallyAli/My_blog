<div class="container">
  <div class="row">
    <div class="col-xs-12 col-md-8">
      <% @posts.each do |post| %>
      <p><%= link_to content_tag(:strong, post.title), post_path(post) %></p>
      <%= post.content %>
      <% end %>
    </div>

    <%= render 'shared/sidebar' %> 

  </div>
</div>